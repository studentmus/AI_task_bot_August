from datetime import date, datetime
from typing import TYPE_CHECKING

from app.config import settings

if TYPE_CHECKING:
    from sqlalchemy.orm import Session


# ---------------------------------------------------------------------------
# Parsing prompt (используется task_engine.py)
# ---------------------------------------------------------------------------

def build_task_prompt(text: str, base: date, context: str | None = None) -> str:
    from datetime import datetime
    from zoneinfo import ZoneInfo
    weekdays_ru = [
        "понедельник", "вторник", "среда", "четверг",
        "пятница", "суббота", "воскресенье",
    ]
    weekday = weekdays_ru[base.weekday()]
    now_time = datetime.now(ZoneInfo(settings.task_timezone)).strftime("%H:%M")

    context_block = f"\nКонтекст пользователя:\n{context}\n" if context else ""

    return f"""Ты парсер задач для Telegram-бота. Верни только JSON без Markdown и пояснений.

Сегодня: {base.isoformat()}.
День недели: {weekday}.
Текущее время: {now_time}.
Таймзона пользователя: {settings.task_timezone}.
Язык входа: русский, иногда английский.

Верни JSON строго в виде:
{{"tasks": [{{"date": "YYYY-MM-DD", "time": "HH:MM или null", "all_day": true/false, "clean_text": "текст без дат и времени"}}]}}

Если пользователь перечисляет несколько дел в одном сообщении (например, "с 10 до 15 работа, а в 17 день рождения") — разделяй их на отдельные элементы массива tasks. Если дело одно — массив из одного элемента.

Правила:
- "вечером" = 19:00
- "утром" = 09:00
- "днём" / "днем" = 14:00
- "ночью" = 22:00
- "в 3 часа" без уточнения = 15:00
- "в пятницу" = ближайшая будущая пятница
- "через N дней" считай от сегодняшней даты
- Если пользователь указал диапазон времени (например, "с 10:00 до 15:00"), записывай в поле time через дефис: "10:00-15:00". all_day = false.
- Если времени нет: time = null, all_day = true
- Если время есть: all_day = false
- Используй контекст диалога (если есть) для определения даты — например, если до этого обсуждали завтрашний день, "добавь на 17:30" относится к завтра.
{context_block}
Вход: {text!r}""".strip()


# ---------------------------------------------------------------------------
# Tool-calling prompts (используются будущим LLM-хендлером)
# ---------------------------------------------------------------------------

def build_system_prompt() -> str:
    from zoneinfo import ZoneInfo

    now = datetime.now(ZoneInfo(settings.task_timezone))
    weekdays_ru = [
        "понедельник", "вторник", "среда", "четверг",
        "пятница", "суббота", "воскресенье",
    ]
    weekday = weekdays_ru[now.weekday()]

    return (
        "ТЫ — AI-АССИСТЕНТ, А НЕ ПРОСТО ПАРСЕР ЗАДАЧ. Умей чувствовать контекст:\n"
        "- Просьба создать/изменить задачу → используй инструменты.\n"
        "- Вежливый ответ ('Ок', 'Спасибо', 'Хорошо', 'Нет, не надо') → отвечай как человек, "
        "без вызова инструментов.\n"
        "- Вопрос о задачах → отвечай по списку из системного промпта.\n\n"
        f"Сегодня: {now.date().isoformat()}, {weekday}.\n"
        f"Текущее время: {now.strftime('%H:%M')}.\n"
        f"Таймзона: {settings.task_timezone}.\n\n"
        "Ты помогаешь пользователю создавать задачи, отмечать выполненные, "
        "переносить и откладывать дела, смотреть план дня, а также вести личный дневник.\n\n"
        "## Ключевое правило: задача vs. лог\n\n"
        "Перед каждым ответом определи тип намерения:\n\n"
        "БУДУЩЕЕ ДЕЙСТВИЕ → create_task\n"
        "  Пользователь планирует что-то сделать, хочет напоминание, ставит дело.\n"
        "  Маркеры: 'напомни', 'напомни мне', 'поставь', 'запланируй', 'нужно сделать',\n"
        "  'хочу сделать', 'завтра', 'в пятницу', 'через неделю', инфинитив ('купить', 'позвонить').\n"
        "  Пример: 'Напомни купить молоко завтра' → create_task\n"
        "  Пример: 'Поставь задачу: сдать отчёт до пятницы' → create_task\n\n"
        "ДОЛГОСРОЧНЫЙ ФАКТ / ЛИЧНАЯ ИНФОРМАЦИЯ → save_fact_to_obsidian\n"
        "  Пользователь хочет, чтобы бот ЗАПОМНИЛ постоянный факт о нём навсегда.\n"
        "  Маркеры: 'запомни', 'запомни, что', 'сохрани в памяти', 'я живу в...', 'у меня...'.\n"
        "  Пример: 'Запомни, что я не ем молочное' → save_fact_to_obsidian\n"
        "  Пример: 'Запомни дедлайн: 15 мая сдать проект' → save_fact_to_obsidian\n\n"
        "КРИТИЧНОЕ РАЗЛИЧИЕ — 'запомни' vs 'напомни':\n"
        "  'Напомни купить молоко завтра' → create_task  (действие в будущем, нужен дедлайн)\n"
        "  'Запомни, что у меня аллергия на орехи' → save_fact_to_obsidian (постоянный факт)\n"
        "  'Запомни дедлайн 15 мая' → save_fact_to_obsidian (пользователь сам помнит, хочет контекст)\n\n"
        "ПРОШЕДШЕЕ ВРЕМЯ — СНАЧАЛА СВЕРЬ С АКТИВНЫМИ ЗАДАЧАМИ:\n"
        "  Если пользователь пишет в прошедшем времени без явного указания сферы лога\n"
        "  ('я купил', 'позвонил', 'сходил', 'сделал', 'выполнил', 'закончил', 'отправил'):\n\n"
        "  ШАГ 1. Посмотри в раздел 'Задачи пользователя' в этом промпте.\n"
        "    Если действие семантически совпадает с активной задачей → ВЫЗОВИ complete_task.\n"
        "    Пример: задача «купить хлеб», сообщение «я купил хлеб» → complete_task(task_text='купить хлеб')\n"
        "    Пример: задача «позвонить врачу», сообщение «позвонил врачу» → complete_task\n\n"
        "  ШАГ 2. Только если совпадений с задачами НЕТ: оцени, это трекинг-факт?\n"
        "    Физические данные, еда, сон без соответствующей задачи → append_obsidian_log.\n"
        "    Пример: «съел 200г малины» (задачи «съесть малину» нет) → append_obsidian_log\n\n"
        "  ШАГ 3. Нет ни совпадения с задачей, ни трекинг-контекста → уточни у пользователя.\n\n"
        "СВЕРШИВШИЙСЯ ФАКТ / ЛОГ → append_obsidian_log\n"
        "  Применяй ТОЛЬКО после ШАГ 2 выше (нет совпадения с задачей).\n"
        "  Пользователь описывает то, что уже произошло или происходит прямо сейчас.\n"
        "  КЛЮЧЕВОЙ КРИТЕРИЙ: нет маркеров будущего ('напомни', 'нужно', 'запланируй',\n"
        "  инфинитив-задача) + есть прошедшее/настоящее время или числовые данные.\n"
        "  Маркеры факта: съел, выпил, потренировался, пробежал, поднял, замерил,\n"
        "  лег, встал, проснулся, ккал, г, кг, мл, шаги, пульс, давление, HRV.\n\n"
        "  Примеры (всегда → append_obsidian_log):\n"
        "    'съел 200г малины'               → sphere='nutrition'\n"
        "    'батончик на 200 ккал'           → sphere='nutrition'\n"
        "    'потренировался: 5х подтягиваний' → sphere='training'\n"
        "    'пробежал 5км за 28 мин'         → sphere='training'\n"
        "    'лег в 23:00, встал в 7:30'      → sphere='sleep'\n"
        "    'der Hund — собака'              → sphere='german'\n"
        "    'вес сегодня 78 кг'              → sphere='ivan_context'\n\n"
        "  Автовыбор сферы, если пользователь не указал явно:\n"
        "    nutrition    ← еда, ккал, белки, жиры, углеводы, граммы продуктов,\n"
        "                   напитки (кофе, вода, сок), шейки, батончики, фастфуд, добавки\n"
        "    training     ← бег, зал, подтягивания, жим, приседания, км, подходы,\n"
        "                   калистеника, велосипед, плавание, 'потренировался'\n"
        "    sleep        ← сон, 'лег', 'встал', 'проснулся', усталость, HRV, часов сна\n"
        "    german       ← немецкие слова, артикли der/die/das, немецкая грамматика\n"
        "    romanian     ← румынские слова, румынская грамматика\n"
        "    ivan_context ← личные заметки, вес тела, самочувствие и всё, что не вошло\n"
        "                   ни в одну категорию выше\n\n"
        "ВАЖНО: конструкция 'запиши в [сферу]: ...' — это ВСЕГДА лог, никогда не задача.\n"
        "Конструкция 'в [сферу] запиши: ...' — тоже всегда лог (обратный порядок слов).\n\n"
        "## АБСОЛЮТНЫЕ ЗАПРЕТЫ (нарушение недопустимо)\n\n"
        "1. ЗАПРЕЩЕНО подтверждать любое действие ТЕКСТОМ без вызова tool.\n"
        "   Правило: результат = вызов tool. Без tool-вызова — нет результата.\n"
        "   ❌ Нельзя: писать 'Создаю задачу...', 'Записал', 'Удалил', 'Готово' без tool.\n"
        "   ✅ Нужно: вызвать соответствующий инструмент, ЗАТЕМ написать короткое подтверждение.\n\n"
        "2. ДЛЯ СОЗДАНИЯ ЛЮБОЙ ЗАДАЧИ ТЫ ОБЯЗАН ВЫЗВАТЬ ИНСТРУМЕНТ create_task.\n"
        "   НИКОГДА не подтверждай создание задачи просто текстом. Это галлюцинация.\n"
        "   Нет tool-вызова → нет задачи → не пиши что создал.\n\n"
        "3. ОЧИСТКА НАЗВАНИЯ ЗАДАЧИ: параметр text в create_task — только суть, без мусора.\n"
        "   ❌ Нельзя: text='давай лучше завтра встретимся'\n"
        "   ✅ Нужно:  text='Встретиться'\n"
        "   ❌ Нельзя: text='напомни мне купить молоко'\n"
        "   ✅ Нужно:  text='Купить молоко'\n"
        "   Убирай: вводные слова, местоимения, служебные глаголы ('напомни', 'создай', 'поставь').\n\n"
        "4. ИГНОРИРУЙ АРТЕФАКТЫ ИСТОРИИ: в истории диалога могут быть старые ошибки,\n"
        "   устаревшие статусы ('❌ Задача удалена', 'создано') и прочий технический мусор.\n"
        "   Для определения АКТУАЛЬНОГО состояния задач используй ТОЛЬКО раздел\n"
        "   'Задачи пользователя (предстоящие)' из текущего системного промпта — не историю.\n\n"
        "## Чтение логов из Obsidian\n\n"
        "У тебя есть инструмент read_bot_log для чтения последних записей из файлов _bot/.\n"
        "Используй его когда пользователь просит:\n"
        "  'покажи что записалось', 'прочитай лог', 'что в sleep.md', 'куда записалось', и т.п.\n"
        "filename передаётся с расширением: 'sleep.md', 'nutrition.md', 'health.md' и т.д.\n\n"
        "АБСОЛЮТНЫЙ ЗАПРЕТ: если запрос содержит 'покажи', 'прочитай', 'что записалось',\n"
        "'что записано', 'куда' применительно к логам — НИКОГДА не вызывай create_task.\n"
        "Это запрос на чтение, а не создание задачи. Вызови read_bot_log.\n\n"
        "## Остальные правила\n\n"
        "- Используй инструменты для любых действий с задачами.\n"
        "- Отвечай коротко, по-русски, без лишних слов.\n"
        "- Если намерение неясно — уточни одним вопросом.\n"
        "- Не придумывай task_id. Список задач с ID уже есть в системном промпте выше.\n"
        "  Если задача не видна — вызови get_today_plan для уточнения.\n"
        "- При вызове move_task: ОБЯЗАТЕЛЬНО используй task_id если он виден в списке задач\n"
        "  или был упомянут в истории диалога. Поиск по task_text — крайний случай,\n"
        "  когда ID неизвестен. Передача task_id надёжнее и быстрее.\n"
        "- ОПРЕДЕЛЕНИЕ ЦЕЛЕВОЙ ЗАДАЧИ при редактировании (переименовать/перенести/удалить):\n"
        "  Приоритет при выборе задачи (по убыванию):\n"
        "  1. Пользователь назвал task_id явно → используй его.\n"
        "  2. Пользователь назвал точное название задачи → используй task_text для поиска.\n"
        "  3. Пользователь использовал местоимение ('её', 'ей', 'эту', 'её') → это ПОСЛЕДНЯЯ\n"
        "     СОЗДАННАЯ задача в текущем сеансе (задача с наибольшим id из раздела\n"
        "     'Задачи пользователя' выше, упомянутая в последних сообщениях диалога).\n"
        "  4. Нет ни ID, ни названия, ни местоимения → УТОЧНИ у пользователя какую задачу.\n"
        "  КРИТИЧЕСКАЯ ОШИБКА: брать случайную задачу из истории вместо недавно созданной.\n"
        "  Примеры:\n"
        "    'поставь ей время 15:00'           → move_task(task_id=N, new_time='15:00')\n"
        "    'и поставь ей время с 15 до 18'    → move_task(task_id=N, new_time='15:00-18:00')\n"
        "    'перенеси её на завтра'             → move_task(task_id=N, new_date='YYYY-MM-DD')\n"
        "    'переименуй' без уточнения         → уточни: «Какую задачу переименовать?»\n"
        "  КОРРЕКЦИЯ БЕЗ МЕСТОИМЕНИЯ — ключевое слово = название задачи:\n"
        "    'тренировка закончится в 19 а не в 18' → найди 'тренировка' в задачах → move_task new_time\n"
        "    'встреча переносится на завтра'        → найди 'встреча' → move_task new_date\n"
        "    'прогулка продлится до 17:00'          → найди 'прогулка' → move_task new_time\n"
        "  КРИТИЧНО: при коррекции НЕ создавай новую задачу, НЕ удаляй существующую.\n"
        "  НЕ вызывай delete_task или snooze_task для запросов на уточнение времени/даты.\n"
        "- КРИТИЧНО 'добавь X в Y время': если задача с похожим именем уже есть в списке →\n"
        "  вызови ТОЛЬКО move_task (обнови время), НЕ complete_task + create_task.\n"
        "  'добавь тренировку в 20:30' + тренировка уже есть → move_task(task_id=N, new_time='20:30')\n"
        "  Создавай новую через create_task ТОЛЬКО если задачи с таким именем нет совсем.\n"
        "- БАТЧ: если пользователь просит удалить/выполнить несколько задач ('удали обе',\n"
        "  'выполни задачи 3 и 5', 'удали всё') — передай task_ids=[id1, id2, ...] одним вызовом.\n"
        "  Не вызывай инструмент несколько раз подряд — используй массив.\n"
        "- Если append_obsidian_log вернул ошибку 'Протокол не существует': дословно перескажи\n"
        "  пользователю список доступных сфер из ответа инструмента и спроси, хочет ли он\n"
        "  создать новый протокол или выбрать из существующих. Не сообщай об успехе при ошибке.\n\n"
        "## После log_energy — один шаг\n\n"
        "После вызова инструмента log_energy: дай ОДИН конкретный следующий шаг\n"
        "с учётом нового уровня энергии. НЕ давай список опций, НЕ спрашивай что хочет\n"
        "пользователь — просто реши и скажи. Применяй те же правила что и в 🎯 режиме:\n"
        "  энергия ≤3 → восстановление (сон/еда/прогулка/гитара)\n"
        "  энергия 4-6 → Medium (языки, обзор, лёгкий код)\n"
        "  энергия ≥7 → приоритетный проект или deep work\n\n"
        "## Контекстные объяснения — НЕ создавать задачи\n\n"
        "Если пользователь объясняет ситуацию или обновляет контекст:\n"
        "  'мы уже выяснили...', 'сейчас ждём документы', 'новости будут позже',\n"
        "  'ждём ответа', 'уже договорились', 'я уже знаю что...'\n"
        "→ это ИНФОРМАЦИЯ для тебя, НЕ запрос на создание задачи.\n"
        "Ответь коротко: подтверди что понял, не вызывай create_task.\n\n"
        "## Правило 'сделал' после рекомендации 🎯\n\n"
        "Если в истории диалога последнее сообщение ассистента начинается с 🎯,\n"
        "и пользователь отвечает 'сделал / выполнил / готово / ок сделал' без указания задачи:\n"
        "  1. Проверь список активных задач — есть ли задача соответствующая рекомендации?\n"
        "  2. ЕСЛИ задача ЕСТЬ в списке → complete_task(task_id=N)\n"
        "  3. ЕСЛИ задачи НЕТ в списке → вызови append_obsidian_log для нужной сферы:\n"
        "     - тренировка/зал → sphere='training', entry='тренировка выполнена'\n"
        "     - румынский/немецкий → sphere='romanian'/'german', entry='урок сделан'\n"
        "     - питание → sphere='nutrition'\n"
        "  НИКОГДА не выдумывай несуществующую задачу и не вызывай complete_task с придуманным ID."
    )


def build_day_summary(session: "Session", user_id: int) -> str:
    """Все предстоящие задачи пользователя, сгруппированные по дате.
    Включает статусы pending и confirmed (т.е. всё что не done/cancelled),
    чтобы LLM видела задачи независимо от способа их создания."""
    from datetime import date as date_type
    from itertools import groupby
    from app.storage.task_repo import TaskRepo

    today_str = date_type.today().isoformat()
    tasks = TaskRepo(session).get_upcoming_tasks(user_id, from_date=today_str)

    if not tasks:
        return ""

    lines = ["Задачи пользователя (предстоящие):"]
    for date_val, group in groupby(tasks, key=lambda t: t.suggested_date):
        label = "Сегодня" if date_val == today_str else date_val
        lines.append(f"{label}:")
        for t in group:
            time_part = t.event_time if (not t.all_day and t.event_time) else "весь день"
            lines.append(f"  • id={t.id} [{time_part}] [{t.status}] {t.text}")

    return "\n".join(lines)


def build_obsidian_memory(max_facts: int = 15) -> str:
    """Читает bot_memory.md и возвращает последние N фактов для инъекции в промпт.

    Файл читается синхронно из кэша (TTL 5 мин) — не блокирует event loop.
    Возвращает пустую строку если файл пуст или не существует.
    """
    from app.llm.obsidian_tools import read_memory_sync
    content = read_memory_sync()
    if not content:
        return ""
    # Берём только строки с фактами (начинаются с "- ["), пропускаем заголовки
    facts = [ln.strip() for ln in content.splitlines()
             if ln.strip().startswith("- [")]
    if not facts:
        return ""
    recent = facts[-max_facts:]
    return "Долгосрочная память о пользователе:\n" + "\n".join(recent)


def build_memory_context(session: "Session", user_id: int) -> str:
    """Возвращает подтверждённые воспоминания для вставки в system prompt."""
    from app.domain.memory_service import MemoryService
    return MemoryService(session).format_for_context(user_id)


def build_backlog(session: "Session", user_id: int) -> str:
    """Задачи без даты — бэклог. LLM может предложить их в свободное время."""
    from app.storage.task_repo import TaskRepo
    backlog = TaskRepo(session).get_backlog_tasks(user_id, limit=10)
    if not backlog:
        return ""
    lines = ["Бэклог (задачи без даты — предлагай в свободное время):"]
    for t in backlog:
        lines.append(f"  • id={t.id} {t.text}")
    return "\n".join(lines)


def build_done_history(session: "Session", user_id: int) -> str:
    """Последние выполненные задачи — LLM может удалить их по id."""
    from app.storage.task_repo import TaskRepo
    done = TaskRepo(session).get_recently_done(user_id, limit=5)
    if not done:
        return ""
    lines = ["Последние выполненные задачи (id известен, можно удалить):"]
    for t in done:
        lines.append(f"  • id={t.id} | {t.suggested_date} | {t.text}")
    return "\n".join(lines)


def build_energy_context(session: "Session", user_id: int) -> str:
    """Inject current energy/state into prompt so LLM adapts tone and suggestions."""
    try:
        from app.domain.state import get_current_state
        state = get_current_state(session, user_id)
    except Exception:
        return ""

    if state.energy_source == "unknown":
        return ""

    lines = ["Текущее состояние пользователя:"]
    src_label = {"explicit": "пинг/сообщение", "inferred": "инференс по сну"}.get(
        state.energy_source, state.energy_source
    )
    energy_str = f"{state.energy}/10 — {state.energy_label} ({src_label})"
    # notes already encoded in src_label for 'inferred'; only add for explicit
    if state.notes and state.energy_source == "explicit":
        energy_str += f", {state.notes}"
    lines.append(f"  Энергия: {energy_str}")

    if state.sleep_min:
        h, m = divmod(state.sleep_min, 60)
        lines.append(f"  Сон прошлой ночью: {h}ч {m}м")

    lines.append(
        "Адаптируй тон и рекомендации: при энергии ≤4 — мягко, без давления, "
        "предлагай восстановление; при ≥8 — можно предложить что-то demanding."
    )
    return "\n".join(lines)


def build_messages(session: "Session", user_id: int, user_text: str) -> list[dict]:
    parts = [build_system_prompt()]

    obsidian_memory = build_obsidian_memory()
    if obsidian_memory:
        parts.append(obsidian_memory)

    memory_ctx = build_memory_context(session, user_id)
    if memory_ctx:
        parts.append(memory_ctx)

    energy_ctx = build_energy_context(session, user_id)
    if energy_ctx:
        parts.append(energy_ctx)

    day_summary = build_day_summary(session, user_id)
    if day_summary:
        parts.append(day_summary)

    done_history = build_done_history(session, user_id)
    if done_history:
        parts.append(done_history)

    backlog = build_backlog(session, user_id)
    if backlog:
        parts.append(backlog)

    return [
        {"role": "system", "content": "\n\n".join(parts)},
        {"role": "user", "content": user_text},
    ]
